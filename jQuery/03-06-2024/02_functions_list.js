// effective function/ methods 

// display 
hide(), show(), toggle()

// fade 
fadeIn(), fadeOut(), fadeToggle(), fadeTo()

// slide 
slideUp(), slideDown(), slideToggle()

// animate 
animate()

// stop
stop()

callback
chaining

// for effected with css 

// css
css()

// css class method 
addClass(), removeClass(), toggleClass()

// to get a class 
hasClass()

// effective with structure / DOM manipulation
// set and get attribute method
text(), html(), val()

// add element 
append(), prepend(), before(), after()

// copy element and move from other place 
appendTo(), prependTo(), insertBefore(), insertAfter()

// copy 
clone()

// remove element 
remove(), empty()

// wrapping element 
wrap(), wrapAll(), wrapInner(), unwrap()

// dimentions 
height(), width(), innerHeight(), innerWidth(), outerHeight(), outerWidth()

// set element attribute
attr(), removeAttr()

// set or get property
prop()

// DOM traversing 

// ancestor 
parent(), parents(), parentsUntil()

// descendent
children(), find()

// siblings
siblings(), next(), nextAll(), nextUntil(), prev(), prevAll(), prevUntil()

// filter 
filter(), first(), last(), eq(), not() 

// etc ... 
